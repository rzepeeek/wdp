d = {5:3, 8:4}
print(d[8])
d[5]=4
d[100] = 2
# napisz funkcje, ktora pobiera lancuch znakow i zwraca jako wynik ile razy wystepuje A i a w tym lancuchu zdan

def ile_liter(lancuch):
    wystapienia = {}
    for litera in lancuch:
        if litera in wystapienia:
            wystapienia[litera] += 1
        else:
            wystapienia[litera] = 1
    return wystapienia

lancuch = input("Podaj lancuch: ")
wynik = ile_liter(lancuch)

# 2 rozwiazanie

def ile_znakow(lancuch):
    slownik = {}
    suma = 0
    for i in lancuch:
        slownik[i] = lancuch.count(i)
    return slownik

print(ile_znakow(slowo))

# LISTA BEZ POWTORZEN

lista = [8, 5, 8, 2, 5, 3, 8, 7]

def unikalnosc(lista):
    wynik = []
    for e in lista:
        if e not in wynik:
            wynik.append(e)
    return wynik

print(unikalnosc(lista))

# CZESTOSC POWTARZANIA SIE

def czestosc(lista):
    wynik = {}
    for e in lista:
        if e not in wynik:
            wynik[e] = 1
        else:
            wynik[e] += 1
    return wynik

print(czestosc(lista))

# CZESTOSC POWTARZANIA SIE W SLOWNIKU

slownik = 'abrakadabra'

def ile_razy(slownik):
    wynik = {}
    for e in slownik:
        if e not in wynik:
            wynik[e] = 1
        else:
            wynik[e] += 1
    return wynik

print(ile_razy(slownik))

# 2 rozwiazanie

def kk(slownik):
    wynik = {}
    for k in slownik:
        if k >= 'a' and k <= 'z':
            wynik[k] = slownik[k]

    return wynik
    isalpha(k)

print(kk(slownik))

Piotruś w klasie na lekcji matematyki bardzo się nudził i pani postanowiła dać mu dodatkowe zadanie do rozwiązania. Piotruś otrzymał kartkę, na której napisane były małe liczby. Piotruś ma za zadanie stwierdzić, czy dana liczba jest palindromem (tzn. czy czyta się ją tak samo od lewej do prawej strony, jak od prawej do lewej). Jeżeli nie, Piotruś powinien dodać do siebie wartość liczby czytanej od lewej do prawej oraz wartość liczby czytanej od prawej do lewej, sprawdzić, czy suma jest palindromem, i jeżeli nie -- kontynuować proces, aż otrzyma palindrom.

Przykładowo, mając daną liczbę 28, Piotruś stwierdzi, że nie jest ona palindromem i wykona dodawanie 28 + 82 = 110. Liczba 110 wciąż nie jest palindromem, zatem Piotruś wykona jeszcze dodawanie 110 + 011 = 110 + 11 = 121. Wynik tego dodawania jest już palindromem, więc obliczenia zostaną zakończone.

Twoim zadaniem jest napisać program, który dla każdej liczby rozważanej przez Piotrusia wypisze palindrom (wynik obliczeń Piotrusia), oraz liczbę dodawań prowadzących do wyniku.

Wejście
Pierwsza linia wejścia zawiera liczbę t (t <= 80), określającą ile liczb znajduje się na kartce Piotrusia. Każda z następnych t linii zawiera dokładnie jedną liczbę naturalną n (1 <= n <= 80), dla której Piotruś musi wykonać obliczenia.

Wyjście
Dla kolejnych liczb podanych na kartce wypisz po jednej linijce zawierającej dwie liczby całkowite oddzielone spacją. Pierwsza oznacza palindrom otrzymany przez Piotrusia, druga -- liczbę dodawań wykonanych, by go otrzymać.



def czy_palindrom(liczba):
    return str(liczba) == str(liczba)[::-1]

def odwroc_i_dodaj(liczba):
    liczba_odwrocona = int(str(liczba)[::-1])
    return liczba + liczba_odwrocona

def znajdz(liczba):
    kroki = 0
    while not czy_palindrom(liczba):
        liczba = odwroc_i_dodaj(liczba)
        kroki += 1
    return liczba, kroki

def main():
    t = int(input("Liczba testów: "))
    
    for _ in range(t):
        n = int(input())
        wynik, kroki = znajdz(n)
        print(f"{wynik} {kroki}")

if __name__ == "__main__":
    main()


Twoim zadaniem jest dodać wszystkie liczby całkowite podane na wejściu.

Wejście
W pierwszym wierszu znajduje się liczba t testów (0 < t < 100) Każdy test opisany jest w następujący sposób. W pierwszym wierszu dana jest liczba n - liczba liczb do zsumowania. Następnie podanych jest n liczb pooddzielanych spacją.

def suma_liczb(liczby):
    return sum(liczby)

def main():
    t = int(input("Liczba testow: "))
    
    for _ in range(t):
        n = int(input())
        liczby_testowe = list(map(int, input().split()))
        
        wynik = suma_liczb(liczby_testowe)
        print(wynik)
        
main()



    
